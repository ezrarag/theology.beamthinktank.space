import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "BEAM Theology & World Religions Institute",
  description: "Exploring interfaith dialogue, religious studies, and spiritual education across traditions",
  keywords: "theology, world religions, interfaith, religious studies, spiritual education, BEAM",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
